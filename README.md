# ore-cli/homebrew-tap

Homebrew tap for [ore](https://github.com/ore-cli/ore), a rebranded,
telemetry-free fork of openai/codex.

```sh
brew install ore-cli/tap/ore
```

`Formula/ore.rb` is generated by ore's release pipeline on every stable release
and pushed here automatically. Edit it in ore, not here.
